import { FormatKeyPipe } from './format-key.pipe';

describe('FormatKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
