import { IpiMultiplierDirective } from './ipi-multiplier.directive';

describe('IpiMultiplierDirective', () => {
  it('should create an instance', () => {
    const directive = new IpiMultiplierDirective();
    expect(directive).toBeTruthy();
  });
});
